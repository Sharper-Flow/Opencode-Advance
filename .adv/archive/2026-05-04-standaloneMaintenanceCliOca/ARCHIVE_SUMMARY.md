# Archive: Standalone maintenance CLI (`oca maintain`) that runs when no OpenCode sessions are active to rebuild plugin dist, merge completed change branches, detect source-drift, run pending migrations, and prepare a clean environment for the next session. Closes Sharper-Flow/Advance#40.

**Change ID:** standaloneMaintenanceCliOca
**Archived:** 2026-05-04T20:05:40.226Z
**Created:** 2026-05-04T18:41:44.992Z

## Tasks Completed

- ✅ [TDD inline] Build `oca maintain` dry-run planner and hard active-state gate in OCA. Add command registration, plan JSON/text model, and session/worker blocker scanning by reusing existing occupancy/session/advruntime primitives. Red: dry-run emits expected plan and execute refuses with fake active blockers. Green: implement `cmd/oca/maintain.go` + `internal/maintain/{plan,session_gate,report}.go`.
  > Added `oca maintain` command registration and command handler, dry-run/execute planning output, hard active blocker refusal, and initial `internal/maintain` plan/session gate primitives. Added RED/GREEN tests for dry-run JSON output and execute blocker refusal. Used fallback evidence for broader verification after one `adv_run_test` incremental WorkflowUpdateFailedError; external `go test ./cmd/oca -count=1` passed and checkpoint recorded.
- ✅ [TDD inline] Add OCA plugin drift detection and build marker support. Detect stale/missing Advance plugin dist relative to source/git SHA, write `<checkout>/plugin/dist/oca-build.json` after successful build, and extend plugin health/doctor checks to report marker freshness. Red: stale marker/missing marker tests fail. Green: implement `internal/maintain/{plugin_drift,rebuild}.go` and marker health checks.
  > Added plugin build marker model and drift detection in `internal/maintain/plugin_drift.go`, including missing-marker, stale, unknown, and fresh statuses. Added marker writing to `<runtime-root>/dist/oca-build.json` with git SHA, source root, branch, dist index, optional worker bundle, and build command hash. Extended plugin health checks with `plugins.advance.build_marker` freshness reporting. Added maintain and health tests for missing marker, fresh marker, and doctor marker pass.
- ✅ [TDD inline][cross-repo: Advance] Add standalone maintenance inspect surface and issue URL validation. Implement `scripts/maintenance/inspect.mjs` with `schema_version: 1`, archived/release-gate eligibility output, and verification summary; add validation so `adv_change_update_issues` rejects non-URL issue refs before corrupting state. Red: shorthand issue refs rejected and inspect schema test fails before implementation. Green: implement scripts/tests in `/home/jrede/dev/oc-plugins/advance`.
  > In isolated Advance worktree `/tmp/opencode/advance-standaloneMaintenanceCliOca`, added standalone maintenance inspect script `scripts/maintenance/inspect.mjs` with schema_version 1, archived release-gate eligibility, and verification summary. Added `plugin/src/maintenance-inspect-script.test.ts`. Hardened `adv_change_update_issues` with explicit GitHub issue URL validation before store mutation and added shorthand rejection regression test. Rebased WIP onto `origin/trunk` (`5f42fcddbcb2bd9c6c3bc56f566b0c3d9040a76b`) before checkpoint.
- ✅ [TDD inline] Implement OCA verified merge and rebuild execute path. Consume Advance inspect JSON, filter only archived release-gate-done candidates, require clean main/worktree and `git merge --ff-only`, rebuild runtime plugins, and retry idempotently after partial build failure. Red: unverified/non-ff candidates skipped and verified candidate action planned. Green: implement `internal/maintain/{advance_state,git,rebuild}.go` execute flow.
  > Implemented OCA Advance inspect consumption and verified merge/rebuild planning. Added `AdvanceInspectReport` parsing via `scripts/maintenance/inspect.mjs`, planner dependency injection, merge candidate filtering on archived release-gate-done evidence, and rebuild actions from plugin drift. Added `ExecutePlan` for execute mode with blocker enforcement, clean git tree + `git merge --ff-only` merge execution, plugin rebuild execution, and build marker writing after rebuild. Wired `oca maintain --execute` to execute allowed actions after plan/blocker reporting. Adjusted build-marker health check to avoid warning for no-build plugins unless marker exists.
- ✅ [TDD inline] Implement conservative worktree cleanup and ADV registry reconciliation integration. Remove only merged, clean, process-free, session-free worktrees; call Advance `scripts/maintenance/reconcile-worktree.mjs` when available; otherwise report follow-up blocker without direct ADV state mutation. Red: dirty/session-active cleanup candidates are blocked. Green: implement `internal/maintain/cleanup.go`.
  > Added conservative cleanup planning with worktree enumeration, eligibility checks for merged, clean, process-free, and session-free worktrees, blocked candidate reasons, and cleanup actions only for eligible change worktrees. Added cleanup execute path that rechecks clean/merged safety, removes worktree, deletes branch, and invokes Advance `scripts/maintenance/reconcile-worktree.mjs` when available without direct ADV state mutation. Added regression test for dirty/session-active cleanup blockers.
- ✅ [TDD inline][cross-repo: Advance] Add runtime loaded plugin diagnostic. Expose loaded plugin module path, process start time, loaded `oca-build.json` marker, worker script path, and host-tool-code reload caveat via `adv_plugin_runtime_info` or `adv_status view=health`. Red: diagnostic test expects marker/path fields. Green: implement in `/home/jrede/dev/oc-plugins/advance`.
  > Added Advance runtime loaded plugin diagnostic to `adv_status view=health` via `plugin_runtime`. It reports loaded module path, process start time, `dist/oca-build.json` marker path/found/data, worker script path, and restart caveat for host-loaded plugin code. Added `plugin/src/utils/plugin-runtime-info.ts` and health-view regression test.
- ✅ [separate verification] Run full cross-repo verification and update docs/spec deltas. Verify OCA with `go test ./...`; verify Advance touched tests/checks; update OCA CLI docs and Advance docs for offline maintenance, build marker, Temporal report-only scope, and runtime diagnostic. Confirm all AC map to tests or documented manual checks.
  > Ran final cross-repo verification: OCA `go test ./...` passed; Advance touched vitest suite `src/tools/status.test.ts src/tools/change.test.ts src/maintenance-inspect-script.test.ts` passed (180 tests); Advance `pnpm run typecheck` passed. Updated OCA README and CLI surface docs for `oca maintain`, offline gates, verified merges, rebuild marker, cleanup behavior, and Temporal report-only scope. Updated Advance README for standalone maintenance inspect surface, issue URL validation, `adv_status view=health` plugin runtime diagnostic, and offline maintenance boundary.

## Specs Modified


## Wisdom Accumulated

- **[pattern]** For plugin build freshness, prefer a committed-runtime marker carrying source git SHA plus build command hash and relative dist paths; health checks can treat missing/stale markers as warnings while `oca maintain --execute` later owns rebuild/remediation.

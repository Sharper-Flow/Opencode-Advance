# Archive: Align OCA with landed and in-progress Advance changes: in-repo archive, agent mesh, worktree absorption, zombie worker heartbeat, foldering cleanup, evidence semantics, and command removals

**Change ID:** alignOcaLandedProgressAdvance
**Archived:** 2026-05-04T01:43:38.529Z
**Created:** 2026-05-04T00:47:33.840Z

## Tasks Completed

- ✅ [KD-C] Add `gh` CLI dependency/auth health check. Affected files: `internal/health/dependency.go`, `internal/health/dependency_test.go`, potentially `cmd/oca/doctor.go`. RED: tests for gh available+authed, gh available+unauthenticated, gh missing. GREEN: probe `gh --version` and `gh auth status` via subprocess/fake binary pattern; return StatusWarn for missing/unauthenticated and StatusPass for authed. Acceptance: AC4, AC8.
  > Added GitHub CLI dependency/auth detection (`temporal.DetectGH`) and surfaced it through `CheckDependencies` as `dependencies.gh_cli`: pass when `gh --version` and `gh auth status` succeed, warning when missing or unauthenticated. Added tests for authenticated, unauthenticated, and missing gh cases; updated dependency checks to return three rows.
- ✅ [KD-D] Implement ADV worker lock heartbeat scanner helper. Affected files: new or existing `internal/advruntime/*worker_lock*.go`, tests. RED: table tests for v2 fresh heartbeat, v2 stale heartbeat with live PID, v2 stale with dead PID, v1/no-last_heartbeat fallback, missing lock, malformed lock. GREEN: scan all `{advRoot}/{projectId}/worker.lock` files; parse heartbeat; derive stale threshold from `ADV_WORKER_HEARTBEAT_STALE_MS` with safe default; never mutate lock files. Acceptance: AC3, AC8.
  > Added read-only ADV worker lock scanner that scans all `{advRoot}/{projectId}/worker.lock` files, handles v1 locks without heartbeat as no-warning fallback, warns on stale v2 heartbeat and malformed heartbeat/lock content, supports env/default stale threshold, and never mutates worker lock files.
- ✅ [KD-D] Integrate worker heartbeat findings into `oca doctor --scope adv-runtime`. Affected files: `internal/health/adv_runtime.go`, `internal/health/adv_runtime_test.go`. Blocked by worker scanner helper. RED: tests mapping stale/fresh/v1 scanner findings to `health.Check` rows. GREEN: append scanner results after existing workflow/worktree checks; stale heartbeat emits StatusWarn with project id/pid/remediation; v1/missing emits no noisy warning. Acceptance: AC3, AC8.
  > Integrated WorkerLockScanner into `CheckAdvRuntime`, appending `adv-runtime.worker-lock.<projectID>` warnings for stale/malformed worker locks while keeping v1/missing locks quiet. Added regression test verifying stale worker heartbeat appears in adv-runtime doctor checks.
- ✅ [KD-B] Add bundle-aware local `.adv/` state health warning. Affected files: likely `internal/health/advance.go`, `internal/health/advance_test.go`, maybe `internal/health/mcp.go` for `Options.ProjectRoot`. RED: fixtures for specs-only pass, valid archive bundle pass, empty/non-bundle `.adv/archive` warn, `.adv/changes`/`.adv/db`/agenda warn. GREEN: scan current repo `.adv`; treat subdirs containing `change.json` as valid archive bundles; warn non-destructively with `adv_migrate_cleanup` hint. Acceptance: AC1, AC6, AC8.
  > Added `Options.ProjectRoot` and repo-local `.adv` scanner to `CheckADVPlugin`. The scanner treats `.adv/specs` and archive subdirs containing `change.json` as valid, warns for `.adv/changes`, `.adv/db`, agenda files, empty archive dirs, and non-bundle archive residue with non-destructive `adv_migrate_cleanup` guidance.
- ✅ [KD-E/F] Reconcile OCA-owned worktree skill as thin defer to ADV `adv-worktree`. Affected files: `assets/skills/worktree/SKILL.md`, relevant skills/docs tests if present or new asset regression test. RED: test or scan fails on active `openchad`, `oc switch`, primary old `worktree_create`, or `git checkout` cleanup guidance in OCA-owned worktree skill. GREEN: keep asset file; state ADV-managed worktrees use `skill("adv-worktree")`; use `adv_worktree_*` names if mentioned; remove stale OpenChad wording and checkout cleanup anti-pattern. Acceptance: AC5, AC8.
  > Rewrote OCA-owned worktree skill as generic/defer guidance: ADV-managed worktrees defer to `skill("adv-worktree")` and `adv_worktree_*` tools, old aliases are marked backward-compatible only, checkout cleanup anti-pattern and active OpenChad wording removed. Added asset regression test preventing stale worktree guidance from returning.
- ✅ [KD-A/G] Update OCA docs for Advance landed changes and ownership boundaries. Affected files: `AGENTS.md`, `STATUS.md`, `NEXT_STEPS.md`. TDD intent: not applicable/docs; verify via text scan. Update `.adv/` architecture/ownership text, in-repo archive bundle reality, landed tense for `boundParentProjectWorkflow` and `repairTemporalMigrationDebt`, heartbeat env vars, no exact `/adv-coordinate` stale refs, and status/next-step guidance for bundle-aware cleanup. Acceptance: AC1, AC2, AC6, AC7, AC8.
  > Updated AGENTS architecture and ownership matrix for `.adv/archive/*/change.json` bundles, clarified OCA worktree skill boundary, documented Advance heartbeat env vars, and changed repair/worker notes to landed wording. Updated STATUS and NEXT_STEPS to refer to landed cleanup tooling and bundle-aware archive preservation.
- ✅ Final verification and related-scan closure. Affected files: all touched. TDD intent: separate verification. Run focused tests from prior tasks, `go test ./...`, `go vet ./...`, and `gofmt -d .`; run text scans for stale active `openchad` in OCA-owned skill, exact `/adv-coordinate`, and `.adv/archive` blanket-legacy wording. Record any ADV-owned `adv-worktree` stale wording as follow-up/wisdom, not OCA mutation. Acceptance: AC8 and full change acceptance.
  > Ran full final verification and closed related-scan findings. Fixed residual stale `open-chad` wording in OCA lgrep skill and bundle-aware archive wording in staged proposal/notes/roadmap docs.

## Specs Modified


// Package health implements the checks invoked by `oca doctor`. Each check
// verifies that a declared component (MCP server, plugin, instruction file,
// agent model, permission rule) is live and consistent with stack.toml.
//
// This package is a scaffold placeholder. Implementation begins in Phase 1
// (MCP checks) and expands through later phases.
package health
